FROM nginx:alpine

# Copy static HTML, JS and CSS (русские имена файлов поддерживаются alpine + utf8)
COPY *.html /usr/share/nginx/html/
COPY api.js /usr/share/nginx/html/

# nginx config tuned for SPA-style routing and UTF-8 filenames
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
